#!/usr/bin/env node

import { generateNames } from "../src/index.js";

const count = Number.parseInt(process.argv[2] ?? "1", 10);
const names = generateNames(Number.isNaN(count) ? 1 : count);

console.log(names.join("\n"));
